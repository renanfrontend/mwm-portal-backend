spring.application.name=bioplanta

# DESATIVA inicializacao de scripts SQL (data.sql)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Configuração do Banco de Dados (Cópia do Azure para garantir segurança no Default)
spring.datasource.url=jdbc:sqlserver://prgsazsqldb.database.windows.net:1433;databaseName=bioplanta;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;quotedIdentifier=OFF;
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.username=bioplantausr
spring.datasource.password=b10pl@nt@$10

# Configuração JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
# GARANTE que o Hibernate NAO altere a estrutura do banco
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true

# Disable DevTools defaults to ensure ddl-auto=none is respected
spring.devtools.add-properties=false

# Porta do Servidor (Azure define a porta via variável PORT, local usa 8080)
server.port=${PORT:8080}

cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5175,http://localhost:5173,*}
